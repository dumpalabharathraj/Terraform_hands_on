region      = "us-east-1"  # Change this to your desired region
bucket_name = "my-grep-bucket"  # Change this to a globally unique bucket name
table_name  = "terraform-state-lock"  # Change this to your desired DynamoDB table name
